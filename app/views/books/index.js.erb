$('#books_repository').append("<%= j render('books', books: @books) %>");
$(".text-center").html("<%= escape_javascript(
    link_to_next_page @books, t('button.view_more'), remote: true, class: "btn btn-primary") %>");
